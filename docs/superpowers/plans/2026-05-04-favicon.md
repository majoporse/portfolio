# Favicon Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace existing favicon with a minimalist "M" initial design that matches the approved spec.

**Architecture:** Create SVG favicon with "M" initial in JetBrains Mono font, add proper favicon links in root.tsx, and provide ICO fallback for legacy browsers.

**Tech Stack:** SVG, React Router links function, ImageMagick for ICO generation

---

### Task 1: Fix favico.svg filename and replace content

**Files:**
- Delete: `public/favico.svg`
- Create: `public/favicon.svg`

- [ ] **Step 1: Remove incorrectly named file**

```bash
rm /home/majoporse/Projects/portfolio/public/favico.svg
```

- [ ] **Step 2: Create proper favicon.svg with "M" initial**

```svg
<svg width="32" height="32" viewBox="0 0 32 32" xmlns="http://www.w3.org/2000/svg">
  <text x="16" y="24" font-family="JetBrains Mono, monospace" font-weight="700" font-size="24" fill="#2D3748" text-anchor="middle">M</text>
</svg>
```

- [ ] **Step 3: Verify SVG displays correctly**

```bash
cat /home/majoporse/Projects/portfolio/public/favicon.svg
```

Expected: SVG file with "M" text element in JetBrains Mono font

- [ ] **Step 4: Commit filename fix and new favicon**

```bash
git add public/favicon.svg
git rm public/favico.svg 2>/dev/null || true
git commit -m "feat: add minimalist M favicon.svg (replace favico.svg)"
```

---

### Task 2: Update root.tsx to add favicon links

**Files:**
- Modify: `app/root.tsx:17-28`

- [ ] **Step 1: Add favicon links to the links function**

Add after line 27 (after the Google Fonts stylesheet link):

```typescript
export const links: Route.LinksFunction = () => [
  { rel: "preconnect", href: "https://fonts.googleapis.com" },
  {
    rel: "preconnect",
    href: "https://fonts.gstatic.com",
    crossOrigin: "anonymous",
  },
  {
    rel: "stylesheet",
    href: "https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&family=JetBrains+Mono:wght@400;700&family=Playfair+Display:ital,wght@1,900&display=swap",
  },
  { rel: "icon", type: "image/svg+xml", href: "/favicon.svg" },
  { rel: "alternate icon", href: "/favicon.ico" },
];
```

- [ ] **Step 2: Verify the edit is correct**

```bash
grep -A 5 "icon" /home/majoporse/Projects/portfolio/app/root.tsx | head -20
```

Expected: Shows the new favicon link entries

- [ ] **Step 3: Commit the root.tsx changes**

```bash
git add app/root.tsx
git commit -m "feat: add favicon links to root.tsx for SVG and ICO fallback"
```

---

### Task 3: Create favicon.ico fallback (optional but recommended)

**Files:**
- Create: `public/favicon.ico`

- [ ] **Step 1: Generate ICO file from SVG**

```bash
# Check if ImageMagick is available
which convert || echo "ImageMagick not found"

# If available, convert SVG to ICO with multiple sizes
convert -background none /home/majoporse/Projects/portfolio/public/favicon.svg -define icon:auto-resize=64,48,32,16 /home/majoporse/Projects/portfolio/public/favicon.ico
```

Alternative if ImageMagick not available:
```bash
# Use rsvg-convert or inkscape if available
which rsvg-convert && rsvg-convert -w 64 -h 64 /home/majoporse/Projects/portfolio/public/favicon.svg -o /tmp/favicon-64.png
```

- [ ] **Step 2: Verify ICO file was created**

```bash
ls -la /home/majoporse/Projects/portfolio/public/favicon.ico
file /home/majoporse/Projects/portfolio/public/favicon.ico
```

Expected: ICO file exists and is recognized as MS Windows icon

- [ ] **Step 3: Commit the ICO file**

```bash
git add public/favicon.ico
git commit -m "feat: add favicon.ico fallback for legacy browsers"
```

---

### Task 4: Verify implementation

**Files:**
- Test: Manual browser verification

- [ ] **Step 1: Run typecheck to ensure no errors**

```bash
cd /home/majoporse/Projects/portfolio && npm run typecheck
```

Expected: No TypeScript errors

- [ ] **Step 2: Start dev server and verify favicon loads**

```bash
cd /home/majoporse/Projects/portfolio && npm run dev &
sleep 5
curl -I http://localhost:5173/favicon.svg
curl -I http://localhost:5173/favicon.ico
```

Expected: Both return 200 OK

- [ ] **Step 3: Final commit if any fixes were needed**

```bash
git status
# If there are fixes, commit them
```

---

## Self-Review Checklist

1. **Spec coverage:** 
   - ✅ "M" initial in JetBrains Mono - Task 1
   - ✅ SVG favicon for modern browsers - Task 1, Task 2
   - ✅ ICO fallback for legacy - Task 3
   - ✅ root.tsx links updated - Task 2
   - ✅ Dark gray #2D3748 color - Task 1

2. **Placeholder scan:** No placeholders found - all steps have actual commands and code.

3. **Type consistency:** N/A - no types used in this plan (SVG + bash commands).
